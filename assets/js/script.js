$("#txtNumber").on("keypress", function(e) {
    if ($("#cmbInput").val() == 1){
        switch ($("#txtNumber").val()) {
            case value:
                
                break;
        
            default:
                break;
        }
    }
});
